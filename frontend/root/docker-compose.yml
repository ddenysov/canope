version: "3.8"

services:
  root-config:
    image: node:24-alpine3.21
    ports:
      - "9000:9000"
    working_dir: /app
    volumes:
      - .:/app
    environment:
      - NODE_ENV=development
    command: ["npm", "start"]
